spring.application.name=springboot-learning
spring.profiles.active=dev

# Server Configuration
server.port=8080
server.servlet.context-path=/${spring.application.name}

# Logging Configuration
logging.level.root=DEBUG
logging.level.com.example=WARN
logging.file.name=logs/springboot-learning.log
logging.pattern.console=%d{yyyy-MM-dd HH:mm:ss} - %msg%n

# Application Info
# app.name=Spring Boot Learning Application
# app.version=1.0.0
# app.description=A basic Spring Boot learning project with annotation-based configuration
